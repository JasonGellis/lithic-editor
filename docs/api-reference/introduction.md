# API Reference

This page is a short index of the reference pages.

- [API Overview](overview.md): the two interfaces and the pipeline steps.
- [Python API](python-api.md): the `process_lithic_drawing` function, its parameters, its return values and examples.
- [CLI Reference](cli-reference.md): the `lithic-editor` commands, flags, output files and exit codes.

For the GUI procedure, see the [User Guide](../user-guide/processing.md).
